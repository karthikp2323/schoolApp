<% n = 0 %>
<% @activities.each do |activity| %>
	<!-- <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
		<div class='widget'>
			<div class="widget-header">
			  <span class='widget-caption'><h><%= activity.title %></h></span> 
	     	<div class='widget-buttons custom-widget-buttons'>
	  	   	<span class="glyphicon glyphicon-time"></span> 
	  	   	<%= time_ago_in_words(activity.created_at) %> ago  &nbsp; &nbsp; 
	  	   	<span class="glyphicon glyphicon-user"></span> By: 
	  	   	<%= activity.school_user.first_name %> 
	  	  </div>
			</div>
	  	<div class="widget-body">
				<% if activity.image.url.include? "assets/products" %>
          <div class="widget_image">
            <%= image_tag activity.image.url, :class => 'imageActivity' %>
          </div>
	     	  <hr class="hr-activities">	
	      <% end %>
				<p><%= activity.message %></p> 
	  	</div>
	  </div>
	</div> -->
	<% if (n += 1) % 2 == 1 %>
		<li>
	<% else %>
		<li class='timeline-inverted'>
	<% end %>
	  <div class="timeline-datetime">
	    <span class="timeline-time">
	      <%= activity.created_at.strftime('%H:%M') %>
	    </span><span class="timeline-date"><%= time_ago_in_words(activity.created_at) %> ago</span>
	  </div>
	  <div class="timeline-badge blue">
	    <i class="fa fa-map-marker"></i>
	  </div>
	  <div class="timeline-panel">
	    <div class="timeline-header bordered-bottom bordered-blue">
	      <span class="timeline-title">
	        <%= activity.title %>
	      </span>
	      <p class="timeline-datetime">
	        <small class="text-muted">
	          <i class="glyphicon glyphicon-time"></i>
	          <span class="timeline-date"><%= time_ago_in_words(activity.created_at) %> ago</span>
	          -
	          <span class="timeline-time"><%= activity.created_at.strftime('%H:%M') %></span>
	        </small>
	      </p>
	    </div>
	    <div class="timeline-body">
	      <% if activity.image.url.include? "assets/products" %>
          <div class="widget_image">
            <%= image_tag activity.image.url, :class => 'imageActivity' %>
          </div>
	     	  <hr class="hr-activities">	
	      <% end %>
				<p><%= activity.message %></p> 
	    </div>
	  </div>
	</li>
<% end %>
<li class="timeline-node">
	<% if @activities.current_page != @activities.total_pages %>
		<% if session[:role_type] == "Admin" %>
	  	<%= link_to 'Load More', new_activity_path(:page => (@activities.current_page + 1)), :remote => true, :id => 'show_more_link', :class => "btn btn-yellow" %>
	  <% elsif session[:role_type] == "Parent" %>
	  	<%= link_to 'Load More', new_activity_path(:page => (@activities.current_page + 1), :class_id => @classroomId), :remote => true, :id => 'show_more_link', :class => "btn btn-yellow" %>
	  <% else %>
	  	<%#= link_to 'Load More', new_activity_path(:page => @activities.current_page + 1, :class_id => @classroomId, :classname => @classname ), :remote => true, :id => 'show_more_link', :class => "btn btn-yellow" %>
	  	<a href='<%= request.original_url + "&page=#{@activities.current_page + 1}" %>' id="show_more_link" class="btn btn-yellow" data-remote="true">Load More</a>
	  <% end %>
	<% end %>
</li>